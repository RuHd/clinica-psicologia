import React from 'react'
import './Header.scss'

const Header = () => {
  return (
    <header>
        <h1>Psych</h1>
        <nav>
            <button>Home</button>
            <button>Contact</button>
        </nav>
    </header>
  )
}

export default Header